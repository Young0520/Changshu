<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.yll.changshu">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".service.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".service.UserMainActivity" android:launchMode="singleTop"/>
        <activity android:name=".service.FirstListActivity"/>
        <activity android:name=".service.SecondListActivity"/>
        <activity android:name=".service.ManagerMainActivity" android:launchMode="singleTop"/>
        <activity android:name=".service.AuditListActivity"/>
        <activity android:name=".service.AuditDetailActivity"/>
        <activity android:name=".service.ApplyListActivity"/>
        <activity android:name=".service.BorrowListActivity"/>
        <activity android:name=".service.ToolListActivity"/>
        <activity android:name=".service.PasswordActivity"/>

    </application>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="uses-permission android:name=“android.permission.ACCESS_NETWORK_STATE”"/>
</manifest>